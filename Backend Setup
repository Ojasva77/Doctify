# Create server directory
mkdir doctify-server
cd doctify-server

# Initialize npm and install dependencies
npm init -y
npm install express mongoose bcryptjs jsonwebtoken dotenv cors morgan
npm install --save-dev nodemon
